[project]
name = "visualization"
version = "0.1.0"
description = "Data visualization notebooks"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.7.0",
    "seaborn>=0.12.0",
    "plotly>=5.14.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
